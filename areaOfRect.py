def area():
    length = float(input("What is the length? "))
    width = float(input("What is the width? "))
    area = length * width

    print(f"The area of a rectangle with dimensions {length} units by {width} units is {area} units squared")

area()